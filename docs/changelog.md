# Changelog

The repository has no versioned release history. The root `CHANGELOG.md` is currently only a title, so this document summarises recent Git history without assigning releases.

| Commit / phase | Recorded change |
|---|---|
| `39869d7` | Merge of real-data rendering fix work. |
| `4bb2aec` | Notification/message counts, messaging error fix, and real document display work. |
| `6d1644d` | Real data use across the application and multi-tab account work. |
| `b0ba090` / `03863e9` | AI recommendation/dashboard work. |
| `8ffff7c` | Dashboard report data/export work. |

For exact history, run `git log --oneline`.
